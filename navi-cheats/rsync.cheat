% rsync

# (ssh) specify explicit path to rsync binary in remote server
rsync --rsync-path=<path-to-binary> -avz -e ssh <source> <destination>

# sync a directory while ignoring common ignores
rsync -avz --exclude '*.swp' --exclude '*~' --exclude '.git/' <source-path> <target-path>

